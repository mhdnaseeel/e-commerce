spring.application.name=sb-ecom

# Database Configuration (Neon production credentials added as defaults)
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-soft-sun-amiqerqe-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_CbMDASt0e5vy}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
spring.app.jwtSecret=${JWT_SECRET:mySecretKey123912738aopsgjnspkmndfsopkvajoirjg94gf2opfng2moknm}
spring.app.jwtExpirationMs=300000000
spring.ecom.app.jwtCookieName=springBootEcom

# Server and URLs
server.port=${PORT:8082}
frontend.url=${FRONTEND_URL:http://localhost:5173/}
image.base.url=${BACKEND_URL:http://localhost:8082}/images

# File Storage
project.image=images/
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# STRIPE KEY
stripe.secret.key=${STRIPE_SECRET_KEY:sk_test_placeholder_please_replace}

# LOGGING
logging.level.com.ecommerce.project=DEBUG
logging.level.org.springframework.security=INFO

# PAYPAL CONFIG
paypal.client.id=${PAYPAL_CLIENT_ID:placeholder_client_id}
paypal.client.secret=${PAYPAL_CLIENT_SECRET:placeholder_client_secret}
paypal.mode=${PAYPAL_MODE:sandbox}
